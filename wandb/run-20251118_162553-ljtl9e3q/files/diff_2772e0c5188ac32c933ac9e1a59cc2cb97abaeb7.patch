diff --git a/nautilus/utils/logger.py b/nautilus/utils/logger.py
index 741a2d3..22c26d0 100644
--- a/nautilus/utils/logger.py
+++ b/nautilus/utils/logger.py
@@ -1,4 +1,5 @@
 from typing import Any
+import wandb

 from torch.utils.tensorboard import SummaryWriter

@@ -51,7 +52,7 @@ class Logger:

         # WandB handles TensorBoard sync automatically if configured,
         # but we can also log explicitly if needed.
-        # if self.use_wandb: wandb.log(metrics, step=step)
+        if self.use_wandb: wandb.log(metrics, step=step)

     def close(self):
         self.writer.close()
